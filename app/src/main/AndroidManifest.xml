<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.heh.catechtest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaTechTEST">
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/border" />

        <activity
            android:name=".Controllers.Test_list_user"
            android:exported="false" />
        <activity
            android:name=".Controllers.RegulationActivity"
            android:exported="false" />
        <activity
            android:name=".Controllers.DataListActivity"
            android:exported="false" />
        <activity
            android:name=".Controllers.ConnexS7Activity"
            android:exported="false" />
        <activity
            android:name=".Controllers.CreateUserActivity"
            android:exported="false" />
        <activity
            android:name=".Controllers.ComprimesActivity"
            android:exported="false" />
        <activity
            android:name=".Controllers.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>